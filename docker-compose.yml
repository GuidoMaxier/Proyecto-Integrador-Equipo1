version: '3.8'

services:
  colab_local:
    build: .
    container_name: colab_local_env
    ports:
      - "8888:8888"
    volumes:
      - .:/home/jovyan/work
    environment:
      - JUPYTER_ENABLE_LAB=yes
    restart: always
